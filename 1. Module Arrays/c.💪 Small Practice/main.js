// 💪 Small Practice
    // Try predicting what this produces before running it:

    
const task = [];
// STEP 1: Add tasks
task.push("Build a Project");
task.push("Practice DOM");
task.push("Practice Arrays");

// STEP 2: Remove the last task
const removedTask = task.pop();

// STEP 3: Add a new task
task.push("Hello maxamad");

// STEP 4: Display everything
console.log("Removed:", removedTask);
console.log(task);
